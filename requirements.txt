<<<<<<< HEAD
# Runtime — needed to construct the Agent and run the evaluator
python-dotenv>=1.0.0     # evaluator loads a local .env if one is present
ollama>=0.3.0            # imported by src/agent.py; the summariser it feeds stays
                        # dormant (falls back to a deterministic string) with no
                        # Ollama server running, which is how every result was measured
numpy>=1.24             # dense-retrieval helpers (inactive without an embedding cache)

# Optional — only for the local LLM message parser / embedding endpoint,
# neither of which is on the scored path
openai>=1.0.0

=======
# Core dependencies
python-dotenv>=1.0.0
openai>=1.0.0

# Retrieval / embeddings
numpy>=1.24.0
nltk>=3.8.0

# Local LLM summarization / query rewriting (Ollama daemon)
ollama>=0.1.0

>>>>>>> 7c1fd1a (clean up code)
# Testing
pytest>=7.0.0
